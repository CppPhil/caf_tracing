TODO: 
 Add JaegerTracing logs / tags / BaggageItems to the server actor.

See: // TODO: Consider making these so that they're called using operator-> 
 -> // Also merge that back to the wrapper branch.

Make it so that the jaeger tracing data stuff
is sent over HTTP
 -> // Merge that back to the wrapper branch.

